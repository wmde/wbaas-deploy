releases:
- name: api
  namespace: default
  chart: ./../../../charts/api
  set:
  - name: app.passport.public
    file: ./../../../oauth-public.key
  - name: app.passport.private
    file: ./../../../oauth-private.key
  values:
  - "values.yaml.gotmpl"

environments:
  default:
    values:
      - ./../../../default.yaml
      - ./../../../private.yaml
