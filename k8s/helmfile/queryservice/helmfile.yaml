repositories:
  - name: wbstack-charts
    url: git+https://github.com/wbstack/deploy/@charts/queryservice?ref=main

releases:
- name: queryservice
  namespace: default
  chart: wbstack-charts/queryservice
  values:
  - "values.yaml.gotmpl"

environments:
  production:
    values:
      - ./../../../default.yaml
      - ./../../../private-production.yaml
  local:
    values:
      - ./../../../private-local.yaml