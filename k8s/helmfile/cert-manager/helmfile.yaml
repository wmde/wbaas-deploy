repositories:
- name: jetstack
  url: https://charts.jetstack.io

releases:
- name: cert-manager
  namespace: cert-manager
  chart: jetstack/cert-manager
  version: v1.5.3
  values:
  - "values.yaml"
  set:
    - name: installCRDs
      value: true

environments:
  # helmfile --environment production diff/apply
  production:
    kubeContext: gke_wikibase-cloud_europe-west3-a_wbaas-1
  # Targetting the kind k8s cluster that can be setup use terraform
  local:
    kubeContext: kind-wbaas-local